name: Hello World
on: [push, workflow_dispatch]
jobs:
  Hello-World-Job:
    runs-on:
      - codebuild-GHA-Deployment-Status-Perm-MXP-${{ github.run_id }}-${{ github.run_attempt }}
      - deployment-status-override:true
    environment: Approvals
    steps:
      - run: echo "Hello World"
      # - run: sleep 2m
