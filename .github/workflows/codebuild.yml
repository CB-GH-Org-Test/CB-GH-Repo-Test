name: Hello World
on: [push, workflow_dispatch]
jobs:
  Hello-World-Job:
    runs-on:
      - codebuild-GHA-GHE-Approval-Workflow-2-${{ github.run_id }}-${{ github.run_attempt }}
      - deployment-status-override:true
      - random-fake-label-test:true
    # environment: Approvals
    steps:
      - run: echo "Hello World"
      # - run: sleep 2m
